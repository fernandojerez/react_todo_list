import styled from "styled-components"
import { onSurfaceColor } from "../styles/theme"

export const SimpleMessage = styled.div`
  font-size: 0.9rem;
  font-style: italic;
  font-family: "Open Sans";
  color: ${onSurfaceColor};
`
